{
  "name": "creatorpath-root",
  "version": "1.0.0",
  "description": "Root package for CreatorPath deployment",
  "scripts": {
    "install-all": "cd backend && npm install",
    "start": "node backend/server.js",
    "build": "npm run install-all"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
